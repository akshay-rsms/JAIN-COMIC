# secrets - NEVER commit
.env.local
.env
*.key

# python
__pycache__/
*.pyc

# large reference-only master character sheets (not used by the pages)
pages/img/*-master.png

# scratch / temp
scratchpad/
*.tmp
*.log

# vercel
.vercel
web/.vercel
web/.env.local

# regenerable intermediate (self-contained pages; web/ is the canonical deploy root)
share/

# os
Thumbs.db
.DS_Store
